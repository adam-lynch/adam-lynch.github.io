const config = require('@vuepress/theme-default');

// module.exports = (options, ctx) => {
//   return {
//     ...config(options, ctx),
//     extends: '@vuepress/theme-default'
//   };
// }

module.exports = config;
